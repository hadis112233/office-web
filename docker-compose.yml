version: '3.8'

services:
  office:
    build: .
    container_name: office-web
    ports:
      - "8080:80"
    volumes:
      - ./data:/var/www/html/data
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
